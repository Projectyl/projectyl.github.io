<div class="feature__wrapper">

  {% for post in site.posts limit:3 %}
    <div class="feature__item{% if include.type %}--{{ include.type }}{% endif %}">
      <div class="archive__item">
	<a href="{{ post.url | relative_url }}" style="text-decoration:none">
        {% if post.header.image %}
          <div class="archive__item-teaser">
		<img src="{{ post.header.image | replace: ".svg", ".png" | relative_url }}"
                alt="{% if post.alt %}{{ post.alt }}{% endif %}">
          </div>
        {% endif %}
        {% if post.header.overlay_image %}
          <div class="archive__item-teaser">
		<img src="{{ post.header.overlay_image | replace: ".svg", ".png" | relative_url }}"
                alt="{% if post.alt %}{{ post.alt }}{% endif %}">
          </div>
        {% endif %}

        <div class="archive__item-body">
	  <i>{{ post.title }}</i>
	  <br>
	  <br>
          {% if post.excerpt %}
            <div class="archive__item-excerpt">
              {{ post.excerpt | markdownify }}
            </div>
          {% endif %}
            <div class="archive__item-excerpt">
              {{ post.date | markdownify | date: "%B %d, %Y" }}
            </div>
        </div>
	</a>
      </div>
    </div>
  {% endfor %}

</div>
